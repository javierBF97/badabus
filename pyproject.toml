[project]
name = "badabus"
version = "0.1.0"
description = "Exploratory, unofficial project on the data of the Badajoz city bus service."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
